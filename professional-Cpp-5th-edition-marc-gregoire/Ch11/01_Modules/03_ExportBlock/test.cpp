/*
 * Chapter 11 - Modules (3/10): Driver for the export-block module
 *
 * Same as sample 02 from the client's point of view - the export style
 * affects how the module declares its surface, not how clients consume it.
 */
import datamodel;

int main()
{
	DataModel::Person p;
	DataModel::Address a;
	DataModel::Persons persons;
}
